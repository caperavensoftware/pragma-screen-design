import {DesignerBase} from "./designer-base";

export class TabsheetDesigner extends DesignerBase {
    constructor(element, eventAggregator) {
        super(element, eventAggregator);
    }

    dispose() {
        super.dispose();
    }
}